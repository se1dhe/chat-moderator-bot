# RedQueen Security — Project Plan (master)

> Единый источник правды по продукту и разработке. Технические детали — в
> [ARCHITECTURE.md](./ARCHITECTURE.md); краткий roadmap — в [ROADMAP.md](./ROADMAP.md).
> Этот файл — мастер: при расхождении верить ему.

Обновлён: 2026-08-17.

---

## 1. Видение и позиционирование

**RedQueen Security** (@RedQueenSecurity_Bot) — публичный SaaS-бот для модерации
Telegram-групп и каналов. Продвинутый аналог @GroupHelpBot с ключевым отличием —
**объяснимой AI-модерацией**: бот не «магически» банит, а кладёт спорное сообщение в
карантин и показывает администратору карточку решения (причина, категория, уверенность
модели, кнопки действий). Человек остаётся в контуре.

Персона и голос — ИИ-система безопасности «Red Queen» из франшизы *Resident Evil*
(«Улей»): холодная, точная, узнаваемая. Отсылки к лору выражены **сильно**, но тексты
остаются понятными и никогда не токсичны к пользователям.

Модель: глобальный SaaS на одном управляемом сервере, freemium **на каждый чат**.

---

## 2. Зафиксированные решения

| Область | Решение |
|---------|---------|
| Скоуп чатов | **Группы + каналы** (комментарии — привязанная группа, подключается как группа) |
| Монетизация | **Free-ядро + Pro** через **Telegram Stars**, гейтинг на уровне чата |
| Языки на старте | **EN + RU + UK** одновременно (i18n с самого начала) |
| Приоритетные killer-фичи | Explainable AI карантин · Raid shield · Adaptive trust score · мультимодальный анти-скам |
| AI runtime | **Qwen через Ollama**, модель **сменная через конфиг** (dev 4B, prod 9B+) |
| AI-хостинг в prod | Решается позже; абстракция `AIProvider` уже это позволяет |
| Данные модерации | **Полный аудит** + удаление/экспорт по запросу (GDPR-обязательства учитываем) |
| Прод-деплой | **VPS + Docker Compose** |
| Mini App | **Позже (M4)**; до этого — команды и inline-кнопки |
| Персона | **Сильно выражена** (лор Resident Evil) |
| Темп | **Качество важнее скорости** — тесты, миграции, чистая архитектура с начала |
| Clone-боты / white-label | **Позже (M5)**; multi-bot архитектуру закладываем заранее |
| БД | PostgreSQL (async, SQLAlchemy 2 + asyncpg), миграции Alembic |
| Кэш/rate-limit/FSM | Redis |
| Dev-инфра | Docker Compose + ngrok для webhook |

---

## 3. Скоуп чатов и права Telegram

Бот работает в супергруппах и каналах. Ограничения платформы, которые определяют
onboarding:

- Для банов/ограничений/удаления боту нужны **права администратора** (Ban users,
  Restrict members, Delete messages).
- При включённом **Privacy Mode** бот не видит весь поток сообщений — для AI-модерации
  и антиспама Privacy Mode **должен быть отключён** (`@BotFather → /setprivacy → Disable`).
- Комментарии в каналах технически являются привязанной discussion-группой и
  подключаются отдельно как группа.

Onboarding обязан проверять наличие прав и статуса Privacy Mode и вести админа за руку.

---

## 4. Фиче-матрица

### 4.1 Паритет с GroupHelp (базовая ценность)

| Категория | Фичи | Тариф |
|-----------|------|-------|
| Защита входа | captcha (кнопка/математика/эмодзи), карантин новичков, авто-кик по таймауту, join-заявки | Free |
| Антиспам | banned words (regex/списки), блокировка ссылок/форвардов/упоминаний, лимиты медиа | Free |
| Антифлуд | счётчики Redis, лимит сообщений/период, авто-мьют | Free |
| Действия | ban, kick, mute (со сроком), unmute, warn, unwarn, unban, purge | Free |
| Warn-система | лимит → авто-действие (mute/kick/ban), активные/погашенные | Free |
| Роли/иммунитеты | exemptions по ролям, whitelist админов | Free |
| Контент | блок sticker/GIF, NSFW-фильтр | Free/Pro |
| Режимы | night mode, silent mode, slow mode | Free |
| Автоматизация | кастомные команды, автоответы, приветствия, правила, пины | Free/Pro |
| Сервис | статистика/тренды/неактивные, перевод, бэкап/экспорт настроек | Pro |

### 4.2 Killer-фичи (дифференциаторы)

| Фича | Суть | Приоритет | Тариф |
|------|------|-----------|-------|
| **Explainable AI карантин** | карточка решения с причиной вместо тихого бана | P0 (M3) | Pro |
| **Raid shield** | детект скоординированных наплывов ботов, авто-лок чата | P0 (M3) | Pro |
| **Adaptive trust score** | репутация по поведению, меньше трения «хорошим» | P1 (M3–M5) | Pro |
| **Мультимодальный анти-скам** | скам-картинки/GIF/голосовые, не только текст | P1 (M5) | Pro |
| Локальный приватный AI | данные не уходят в облако, дёшево масштабируется | сквозное | — |
| Персона RedQueen | узнаваемый UX/голос | сквозное | — |

---

## 5. Архитектура (сводка)

```
Telegram ──(polling dev / webhook+ngrok)── aiogram 3 Dispatcher
                                  │  middlewares: DB session · i18n · throttling · ACL
                                  │  routers: common · moderation · settings · ai_review · payments
                                  ▼
                            services/ (бизнес-логика, без aiogram)
              ┌───────────────────┼───────────────────┐
          db.repo (Postgres)   ai.provider          redis
                             (Ollama/Qwen | rules)  (rate-limit, FSM, raid-счётчики)
```

Хендлеры тонкие: апдейт → сервис → ответ. Вся логика в `services/`, чтобы переиспользовать
из будущего Mini App API и покрывать тестами. Полный технический разбор — в ARCHITECTURE.md.

**Multi-bot задел (для M5 white-label):** все данные ключуются по `chat_telegram_id`; токен
бота и идентичность выносятся в конфиг-слой, чтобы позже поднимать несколько экземпляров
над общей БД без переписывания домена.

---

## 6. Модель данных

Ядро (реализовано в M1): `Chat`, `ChatSettings` (JSONB для гибких полей), `User`
(поле `trust_score` под Adaptive trust), `Warn`, `ModAction` (аудит), `AIVerdict`
(объяснимые вердикты), `Subscription` (Pro на чат).

Расширения по релизам: `Payment` (ledger Stars, M4), `CaptchaSession` (M2),
`RaidEvent` (M3), `MediaVerdict` (M5), `CustomCommand` / `AutoReply` (M2–M3).

Все таблицы с `chat_telegram_id` индексируются по нему; аудит и вердикты партиционируемы
по дате. Retention: полный аудит по умолчанию, удаление/экспорт по запросу админа.

---

## 7. AI-пайплайн

1. Быстрые rule-фильтры (banned words, ссылки, флуд) — синхронно, дёшево.
2. Неоднозначное → очередь AI-анализа (async, лимит на чат, graceful fallback при
   недоступности Ollama → `RuleProvider`).
3. `AIProvider.classify_text()` → категория (ok/spam/scam/toxicity/nsfw/flood), score
   0–100, короткое объяснение.
4. По режиму чата и порогу: `off` (только лог) · `quarantine` (карточка решения) ·
   `autoban` (Pro, высокий порог, авто + лог).
5. Мультимодальность (M5): vision для изображений/GIF, ключевые кадры видео, ASR
   голосовых, безопасный extractor документов — через ту же очередь.

Runtime — абстракция: смена модели (qwen3:4b → qwen3:8b/14b) через env без изменения кода.

---

## 8. Монетизация (Telegram Stars)

- **Free**: базовая модерация, captcha, антифлуд, warns, кастомные команды (лимит).
- **Pro (на чат)**: AI-модерация (quarantine/autoban), Raid shield, trust score,
  расширенная аналитика, бэкапы, мультимодальность, без лимитов на команды/автоответы.
- Оплата — Telegram Stars (XTR) внутри бота/Mini App. Нужен проверяемый **ledger**
  (`Payment`) и обработка возвратов. `Subscription.active_until` гейтит фичи; при
  истечении чат откатывается на Free без потери данных.

---

## 9. Приватность и соответствие

Полный аудит действий и вердиктов включён по умолчанию (ценность для админов и
разбора инцидентов). Обязательные контроли: удаление данных чата по запросу, экспорт,
настройка retention на уровне чата. Секреты только в `.env` (в `.gitignore`), в код и
логи не попадают. При работе в ЕС-аудитории держим в уме GDPR (право на удаление,
минимизация в текстах вердиктов).

---

## 10. Интернационализация

EN + RU + UK с первого релиза. Строки персоны и системные тексты выносятся в
per-locale каталоги; язык определяется настройкой чата (`Chat.lang`) с фолбэком на
`DEFAULT_LANG`. Персона RedQueen адаптируется под каждый язык, сохраняя тон.

---

## 11. Деплой и окружения

- **Dev (сейчас):** локально, polling; Postgres/Redis в Docker; Ollama нативно на
  Apple Silicon (GPU). Webhook — через ngrok при необходимости.
- **Prod:** VPS + Docker Compose (postgres, redis, bot; ollama — отдельный профиль/сервер).
  Webhook на постоянном домене с `WEBHOOK_SECRET`. Миграции — Alembic (не `create_all`).
- Схема секретов и переменных — в `.env.example`.

---

## 12. Релизы

Легенда критериев готовности (DoD): код + миграции + тесты ключевой логики + запись в
CHANGELOG + ручная проверка в тестовом чате.

### M1 — Каркас и базовая модерация ✅ (сделано)
Конфиг, БД+модели, ядро aiogram, DI-мидлвари, ACL-фильтр админа, `/start`+`/help`,
`ban/kick/mute/unmute/warn/unwarn/unban/purge`, warn-лимиты с авто-действием, настройки
чата, аудит-лог, AI-listener с карточкой карантина (rule-fallback), Docker/Alembic.
**DoD:** ✅ команды работают, логика покрыта smoke-тестами, всё компилируется.

### M2 — Защита входа, антиспам, i18n ✅ (сделано)
- Captcha (кнопка/математика), карантин новичков, авто-кик по таймауту (DB-driven
  sweeper, переживает рестарт), join-заявки через ЛС.
- Антифлуд на Redis (фиксированные окна), блок ссылок/форвардов/упоминаний, блок медиа
  по типу (sticker/animation/voice/video_note).
- Banned words (список), night/silent/slow mode, exemptions по ролям (whitelist +
  кэш админов чата в Redis).
- i18n-каркас (EN/RU/UK) — `locales/` + `t(lang, key, **kw)`, `LangMiddleware`
  резолвит `Chat.lang` / `language_code`, команда `/lang`.
- Онбординг: приветствие при добавлении/повышении, проверка прав, `/checksetup`
  (Privacy Mode — только инструкция, Bot API не даёт способа проверить статус).
- Alembic — первая ревизия под схему M1 + ревизия под `CaptchaSession`; `create_all`
  убран из `__main__.py`.
- **DoD:** ✅ новичок проходит captcha; флуд/ссылки блокируются; тексты на 3 языках;
  32 pytest-теста на сервисный слой (i18n, config, filters, antiflood, modes, captcha).

### M3 — AI-модерация (killer P0) ✅ (сделано)
- AI-очередь: глобальный `asyncio.Semaphore` (`AI_MAX_CONCURRENCY`) + лимит запросов на
  чат в минуту (Redis fixed-window, `config.ai.max_per_minute`) перед вызовом Ollama.
  AI — последний этап message-pipeline (после antiflood/filters/modes), т.е. уже
  действует как fallback для того, что не поймали быстрые правила.
- Explainable-карточка вердикта: третья кнопка **«Правило»** — превращает пойманный
  текст в запись `filters.banned_words`, чтобы повтор ловился быстрым фильтром без AI.
- **Raid shield**: Redis-окно по join-событиям на чат → авто-лок (блок сообщений
  не-админов) с cooldown, алерт в чат, аудит-таблица `RaidEvent`, команды
  `/raidshield`, `/raidconfig`, `/unlock`.
- Каркас **Adaptive trust score**: `services/trust.py` — бан/кик/мьют/warn штрафуют,
  прохождение капчи и отмена ложного AI-вердикта поощряют `User.trust_score`;
  команда `/trust`. Влияние на строгость проверок — отдельная будущая веха.
- **DoD:** ✅ AI-карантин работает end-to-end (карантин/autoban/rule); raid-лок
  срабатывает на симуляции наплыва join'ов; 40 pytest-тестов.

### M4 — Mini App (TMA-first) и монетизация ✅ (сделано)
> **Разворот приоритетов (2026-08-16):** управление становится **TMA-first** — Mini App
> это основная поверхность, а не команды. Всё максимально настраиваемо в TMA, качество
> премиальное; монетизация — поверх уже доставленной ценности (сначала ценность, потом
> Stars). Дизайн адаптирован с соседнего проекта **CP-helper** (тёмно-красная тема).

- ✅ **Backend API** (`src/redqueen/api/`, aiohttp): работает в polling и webhook; авторизация
  Telegram `initData` (HMAC) + гейт «админ чата»; эндпоинты — список чатов, чтение/запись
  всех настроек (`config.full_view`/`apply_patch`), аудит, очередь карантина, статистика;
  раздача собранного TMA с того же origin (`/app`).
- ✅ **Mini App** (`webapp/`, React 19 + Vite): дашборд + все настройки редактируемо
  (captcha, antiflood, filters, modes, AI incl. per-category пороги, raid, warns,
  exemptions), очередь карантина (Approve/Ban/Rule), аудит, статистика; EN/RU/UK;
  подключение через menu button / `/panel`.
- ✅ **AI-эффективность**: контекст reply-to + language-aware few-shot промпт, Redis-кэш
  вердиктов, per-category пороги с поправкой на trust-score, ре-скан правок.
- ✅ **Монетизация**: Telegram Stars (XTR) — `services/billing.py` (is_pro/activate/
  record_payment), `handlers/payments.py` (/pro, /subscription, invoice, pre_checkout,
  successful_payment), `Payment` ledger (идемпотентный по charge_id). Гейтинг Pro-фич
  (`billing.PRO_FEATURES` = AI autoban · raid shield · analytics) в рантайме; ядро
  модерации бесплатно. В TMA — Pro-баннер и апгрейд через `openInvoice`.
- **DoD:** ✅ TMA управляет всеми настройками end-to-end; ✅ оплата Stars включает Pro
  (на чат, с продлением и аудитом).

### M5 — Мультимодальность, масштаб, white-label  ← следующий этап
- Vision (картинки/GIF), ключевые кадры видео, ASR голосовых, document extractor.
- Adaptive trust score в полном объёме, кросс-чат репутация, шаринг чёрных списков.
- Clone-боты / white-label (multi-bot над общей БД).
- **DoD:** скам-картинка ловится; можно поднять второй бренд-бот из конфига.

---

## 13. Стратегия качества

Темп — «качество важнее скорости». Практики: Alembic-миграции с M2 (не `create_all` в
prod), pytest на сервисный слой и фильтры, `Dispatcher.feed_update()` для интеграционных
тестов хендлеров, ruff-линт, типизация, аудит-лог как основа воспроизводимости решений.
Навык Codex `aiogram-3-development` запрещает API 2.x — держимся его правил.

---

## 14. Риски и ограничения

- **Telegram API:** права/Privacy Mode обязательны — снимаем онбордингом.
- **Ресурсы AI:** локальная модель упирается в RAM/GPU — модель сменная, есть fallback.
- **Скам эволюционирует:** правила стареют — AI + обновляемые списки, обратная связь от админов.
- **Приватность:** полный аудит ↔ GDPR — обязательны экспорт/удаление и минимизация в вердиктах.
- **Стоимость масштаба:** очередь и лимиты на чат, чтобы AI не заваливал сервер.

---

## 15. Открытые вопросы (решить по ходу)

- Прод-хостинг AI (свой GPU vs аренда) — к M3/M4.
- Тарифная сетка Pro в Stars (цена/период) — к M4.
- Точные категории/пороги AI и наборы banned words per-locale — к M2/M3.
- Нужен ли отдельный веб-дашборд помимо Mini App — к M4.

---

## 16. Следующий шаг

**M1–M4 сделаны** (TMA-first управление + Telegram Stars монетизация). Начинать **M5**:
мультимодальный анти-скам (vision/voice), полный Adaptive trust score, кросс-чат
репутация, clone-боты / white-label. Разработка продолжается в Claude Code — контекст
репозитория и команды см. в [`CLAUDE.md`](../CLAUDE.md).
